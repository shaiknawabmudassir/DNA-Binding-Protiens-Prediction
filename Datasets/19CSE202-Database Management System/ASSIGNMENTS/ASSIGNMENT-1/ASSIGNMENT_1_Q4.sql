USE ASSIGNMENT_1;

CREATE TABLE ORDERS(
ORDER_NO INTEGER PRIMARY KEY,
CUSTOMER_NO INTEGER NOT NULL
);

INSERT INTO ORDERS()
VALUES(1, 1),
      (2, 2),
      (3, 3),
      (4, 3);
      
SELECT CUSTOMER_NO FROM ORDERS GROUP BY CUSTOMER_NO ORDER BY COUNT(*) DESC LIMIT 1;