USE ASSIGNMENT_2;

CREATE TABLE STATION(
ID INTEGER PRIMARY KEY,
CITY VARCHAR(21) NOT NULL,
STATE VARCHAR(2) NOT NULL,
LAT_N INTEGER NOT NULL,
LONG_W INTEGER NOT NULL
);

INSERT INTO STATION()
VALUES (001, 'BENGALORE', 'KA',12,23),
       (002, 'MYSORE', 'KA', 15,16),
       (003, 'NELLORE', 'AP', 14,56),
       (004, 'KADAPA', 'AP', 16,59),
       (005, 'BANGLORE','KA',18,89),
       (006, 'NELLORE', 'AP', 19,65),
       (007, 'INDORE', 'MP', 56,98);
	
SELECT CITY, STATE FROM STATION;

SELECT CITY FROM STATION WHERE MOD(ID,2) = 0 ORDER BY CITY;

SELECT COUNT(CITY) - COUNT(DISTINCT CITY) FROM STATION;

SELECT CITY FROM STATION WHERE CITY LIKE 'A%'OR CITY LIKE 'E%' OR CITY LIKE 'I%' OR CITY LIKE 'O%' OR CITY LIKE 'U%';

SELECT CITY, LENGTH(CITY) FROM STATION ORDER BY LENGTH(CITY) ASC, CITY LIMIT 1;
SELECT CITY, LENGTH(CITY) FROM STATION ORDER BY LENGTH(CITY) DESC, CITY LIMIT 1;